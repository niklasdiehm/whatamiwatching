import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: "100%",
  },
  description: {
    padding: 5,
    fontSize: 18,
  },
});

export default styles;